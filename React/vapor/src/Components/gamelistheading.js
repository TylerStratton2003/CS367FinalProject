import React from "react";

const gamelistheading = (props) => {
    return(
        <div className="col">
            <h1>{props.heading}</h1>
        </div>
    )
}

export default gamelistheading;
